import Loader from '@/components/global/Loader';

const Loading = () => {
  return (
    <div className='flex justify-center mx-auto container mt-20'>
      <Loader />
    </div>
  );
};

export default Loading;
